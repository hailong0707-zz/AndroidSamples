<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elbbbird.android.core">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <provider
            android:name=".servicemanager.ServiceManagerProvider"
            android:authorities="com.elbbbird.android.core.servicemanager.ServiceManagerProvider"
            android:exported="false"
            android:process=":servicemanager" />
    </application>

</manifest>
